# Active Projects

Use this folder for projects currently being worked.

Each active project should have a project brief and session logs, either directly in the project folder or linked from `docs/progress-tracker.md`.

Recommended structure:

```text
project-name/
  project-brief.md
  sessions/
  debugging/
  artifacts/
```

